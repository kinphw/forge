# Sentinel-Forge Task Log

> 모든 AI와 사용자가 공유하는 전역 작업 로그입니다.
> 최신 항목을 맨 위에 추가합니다. 기존 기록은 수정하지 말고, 정정이 필요하면 새 항목으로 남깁니다.

## 기록 규칙

- 형식: `YYYY-MM-DD HH:MM:SS ±TZ | 작업주체 | 작업내용`
- 작업주체 예시: `User`, `Claude Code`, `GPT Codex`
- 한 항목은 1~2줄 수준으로 간결하게 작성
- 설계 결정, 문서 추가, 기능 구현, 상태 변경만 기록

## Log

- 2026-07-21 +09:00 | Claude Code | **[최종] Cursor 모드 정렬 재활성 — "빈 문단 2개 가드"로 기존 내용 보호 (사용자 제안)**. 직전 항목(무정렬 비활성)을 사용자 피드백("공백으로 안전거리 두면 되지 않나 + 보통 마지막에 삽입")으로 재구현. **실측 세분화**(`cursortest end|mid` 인자 추가): END 삽입(흔한 경우, 커서 아래 기존 없음)은 가드 없이도 기존 내용 무접촉 확인, MID 삽입만 오염. **가드 구현**(`GenerateHwpxViaCom` Cursor 분기): 삽입 전 `BreakPara→MoveUp→BreakPara→MoveUp` 로 **빈 문단 2개** 생성 후 위쪽에서 삽입 시작 — 삽입 내용이 위 빈 문단을 소비하며 자라고 **아래 빈 문단이 가드로 남아** 마지막 노드 정렬의 ≤1문단 오버슛을 흡수(빈 문단은 linter skip). 1개 가드는 삽입 지점에 소비돼 무효(실측)라 2개 필수. **검증**: MID·END 모두 기존 내용에 IndentAlign `[line]` 0건. 부작용: 삽입 끝에 빈 줄 1개 잔존(자연스러운 간격). **한계 기록**: Cursor 모드 삽입 내용의 정렬은 박스 exit 캐럿 drift 로 best-effort(일부 문단이 빈 문단 skip 으로 빠질 수 있음) — 완벽 정렬 원하면 삽입 후 선택+Ctrl+Shift+Q. 앞서 발견한 "정렬 OFF 인데 indent 잔존" 미스터리는 **한/글 세션 typing 문단모양 상속**(이전 변환 잔재)으로 해명 — cursortest 의 walk indent 판독은 이 상속 때문에 신뢰 불가, 판정은 변환 로그 `[line]` 기준. X(선택→md)도 동일 경로라 가드+정렬 적용됨(사용자가 원한 일관성). New 모드 최종 회귀: 결론박스 Indent=-4750·표 가운데 무변경 정상. sln 빌드 0/0.
- 2026-07-21 +09:00 | Claude Code | **[정정] Cursor 모드 정렬은 기존 내용 오염으로 비활성 — 체크박스는 삽입만, X 도 현행 무정렬 유지**. 앞 두 항목(Cursor 정렬 지원·X 에 Q 적용)을 실측으로 뒤집음. **실측 도구**: `Forge.Probe cursortest` 신설 — FileNew→'아래 기존' 긴 □글머리 삽입→MoveDocBegin(커서 위로)→Cursor+Q 변환→기존 내용 오염 여부 판정. **발견**(변환 로그 `[line] paraBegin=(0,5,0) text=□ △아래기존`): Cursor 정렬 시 IndentAlign 이 **커서 아래 기존 문단까지 처리해 오염**. 근본 원인 = IndentAlign.ProcessParagraph 의 마커/본문 워드 탐색이 **다음 문단으로 오버슛** — New(append)는 다음이 빈 문서끝이라 무해하나 Cursor 는 다음이 기존 내용이라 치명. 셀 정렬만 꺼도 재발(평문단 정렬도 오버슛). **결정**: `alignInline = mode==New && ...` 로 되돌려 **Cursor 모드 무정렬**(삽입만), `ConvertSelectionToHwpx`(X) 도 원래대로(무정렬). 확인: Cursor 변환 로그에 `[line]` 0건(IndentAlign 미실행=기존 내용 안전), New 모드 정렬 정상(결론박스 Indent=-4750, 표 가운데 Indent=0). **잔존 개선안**: Cursor 정렬하려면 IndentAlign 오버슛 격리(삽입 끝에 sentinel 빈 문단) 필요 — 별도 과제. **부수**: DispatchOne 반환을 정렬 종류(AlignCurrentPara/AlignNone/셀id)로 세분(New 인라인 정렬을 범위순회 없이 노드별 문단/셀로 — 코드 정리), AlignBodyRange 제거, cursortest 에 문단 AlignType/Indent 판독. sln 빌드 0/0.
- 2026-07-21 +09:00 | Claude Code | **마크다운 탭 "현재 파일 현재 캐럿에 생성" 체크박스 — Cursor 모드 정렬 지원**. 사용자 요청: 마크다운 변환이 무조건 새 문서였는데, 체크 시 현재 문서 현재 캐럿에 삽입. 근거(사용자): 정렬 로직이 whole-doc 왕복이 아니라 "쭉 흘러내려가는" 노드별 local 로 바뀌어 Cursor 삽입이 안전해짐. **구현**: (1) `MarkdownTab` 에 체크박스 `_insertAtCursor`(기본 해제) + 툴팁 + `_tooltip` 필드. `OnConvert` 가 `mode = 체크 ? Cursor : New`, 완료 메시지/저장 흐름 분기(Cursor 는 한/글에서 직접 저장, 다른이름저장 버튼 비활성 유지). (2) `HwpxWriter.alignInline` 게이트 `mode==New &&` 제거 → **New·Cursor 둘 다 인라인 정렬**(두 플래그 켜졌을 때). (3) `AlignBodyRange` 에 `endIsDocEnd` 파라미터 — New(append)=MoveDocEnd, **Cursor=노드 직후 현재 캐럿**(=삽입 끝; MoveDocEnd 쓰면 문서 맨끝이라 기존 내용까지 정렬하는 사고). 순회는 endPos.Para 까지만(기존 문서 보호), 끝에 캐럿을 endPos 로 복원(정렬이 옮긴 캐럿 → 다음 노드가 삽입 뒤에서 이어 쓰도록; Cursor 필수). (4) `ConvertSelectionToHwpx`(X 선택→md, 2곳) → **X 도 Q(들·자·들) 적용**(사용자 후속 요청: X와 마크다운 변환의 차이는 Q 유무뿐이니 일관되게). 공용 Cursor 인라인 정렬 경로 재활용(코드 중복 0) — 삽입 구간만 처리해 안전. sln 빌드 0/0. ★ 한/글 종료로 실측 미완 — New(체크 해제) 회귀 + Cursor(체크) 삽입·정렬·기존내용 무변경 + X 선택변환 정렬 확인 필요.
- 2026-07-21 +09:00 | Claude Code | **0.5.9 릴리즈 — Bold 규칙 `__`→`**`(마크다운 표준) 반영 검증 + 릴리즈**. 다른 세션이 Bold 인라인 규칙을 `__X__` 에서 `**X**`(canonical) 로 전환(markdown-spec v1.7). 릴리즈 전 정합성 검증: (1) `Primitives.BoldTokenRegex = (\*\*|__)(.+?)\1` — `**X**`+`__X__`(하위호환) 둘 다, `\1` 역참조로 같은 짝만 매칭. `InsertText` 가 매치 밖 plain / Groups[2] 만 CharShapeBold. (2) `Parser`: 참조주석 `AnnotationRefRegex ^(\*+)\s+` — 구식 다중참조 `**`/`***` 입력도 받되 마커를 **단일 `*` 로 정규화**(v1.7 다중참조 폐지). 공백 가드로 `**강조**`(별표 뒤 비공백) 볼드와 줄머리 참조가 충돌 안 함. `AnnotationRenderer` doc 도 일관. (3) 테스트 17/17(볼드 케이스 포함), end-to-end 변환 스모크(볼드·하위호환·혼용·참조·결론박스 내 볼드) 크래시 0. **릴리즈**: 0.5.8.zip 이 이미 존재(stale)라 버전 충돌 회피 위해 `build release` 로 0.5.8→**0.5.9** bump + publish + Forge-v0.5.9.zip(592KB). 이 릴리즈에 이 세션 미커밋분(결론박스 회색화·단일 하향 패스 정렬·tabledump)도 함께 포함.
- 2026-07-16 +09:00 | Claude Code | **마크다운 변환 정렬을 단일 하향 패스로 통합 (쓰기+정렬 인라인)**. 사용자 지적: 직전 구현이 "1.내용생성 → 2.본문 전체 순회 정렬 → 3.박스 셀만 순회 정렬" 로 문서를 여러 번 훑음. 의도는 2·3 을 단일 흐름으로 하나씩 내려가며 처리("문서 두 번만" 또는 "1+2+3 한 번에"). **채택**: 1+2+3 통합 = **각 노드를 쓴 직후 그 자리에서 (들·자·들) 정렬**. 정렬은 문단 모양·자간만 바꾸고 텍스트/표 구조는 안 건드려 append 흐름을 안 깬다(위험 낮음). **구현**(`HwpxWriter`): (1) `DispatchOne` 반환형 void→**int**(정렬 필요한 박스 셀 list-id, 없으면 -1; 결론박스만 셀 반환, 표는 가운데정렬이라 -1). (2) `DispatchNodes` 에 `alignEachNode`/`alignLog` 파라미터 — 노드 쓰기 전 `nodeStart` 캡쳐, DispatchOne 직후 `AlignBodyRange(nodeStart~docEnd 본문 문단)` + 셀 반환 시 `ApplyOverLists`(그 셀만). hwp dynamic 이라 CombinedParaAction 은 `ParaActionFn` 변수 경유(CS1976 회피). (3) `AlignBodyRange` 신설(기존 StageTwoCombined 의 본문 루프를 범위 한정판으로) + `StageTwoCombined` 제거. `CombinedParaAction`(들·자·들 SSOT) 유지. (4) `GenerateHwpxViaCom`: `alignInline = New && 두 정렬 모두` 면 인라인, 후처리 STAGE 2 블록은 단일 플래그(들만/자만, 드묾)만 담당. 취소(isCancelled)는 인라인 정렬·셀 정렬까지 전달. **동등성**: 정렬은 문단별 독립이라 "쓰자마자 정렬" = "다 쓰고 정렬" 결과 동일(append-only 라 뒤 노드가 앞 문단 wrap 을 안 바꿈). sln 빌드 0/0. **실측 검증**(probe convert + tabledump 문단 정렬 판독): 결론박스(#11) AlignType=0 Indent=-4496(정렬 적용됨), GFM 표 셀 8칸(#3~#10) AlignType=3(가운데) Indent=0(무변경), 본문 글머리 인라인 정렬(로그 [line] 확인), 별도 STAGE 2 순회 없음. tabledump 에 문단 AlignType/Indentation 판독 추가(영구 진단).
- 2026-07-16 +09:00 | Claude Code | **마크다운 결론박스(=>) 기본 시각 교체 — 민트 점선 → 회색 화살표박스(양식삽입 #10)**. 사용자 요청: "결론박스가 지금 초록색으로 생성되는데 참고박스 중 [10] 화살표박스(회색)을 기본으로". **구조는 유지, 시각 속성만 교체** — ConclusionRenderer 에는 "정렬을 Right→Justify 로 바꿔 CloseEx/MoveLineDown 캐럿 cascade 버그를 고친" 이력이 주석에 박제돼 있어, SSOT 위임(SectionRenderer→중제목_그라인드 패턴)으로 갈아엎으면 그 fix 를 회귀시킬 위험이 있어 채택하지 않음. **변경**: (1) `ReportSpec` 결론 필드 — `ConclusionBgRgb` 민트(205,242,228)→**#F2F2F2(242,242,242)**, `ConclusionBoxHeightMm` 18→**17**, `ConclusionBorderDotted` true→**false**(실선), 신규 `ConclusionMarker`(기본 **➡**, 이전 ⇨)·`ConclusionBorderWidth`(1 ≈ 0.12mm)·`ConclusionBorderRgb`(검정). (2) `ConclusionRenderer` — 하드코딩이던 마커/굵기를 spec 참조로, `SetTableBorderColor` 추가, 표 폭 기준 199.5→**208.0 - 여백**(#10 화살표박스와 동일). AlignPara(Justify)는 위 cascade fix 때문에 그대로 유지(#10 은 Left 지만 한 줄 짧은 본문이라 시각차 미미). **Q 정렬 호환 확인**: 새 마커 `➡`(U+27A1)는 이미 `IndentAlign.BulletMarkers` 에 등재돼 있어 STAGE 2 박스 셀 정렬이 마커를 계속 인식. 민트 점선으로 되돌리려면 spec 4개 필드만 원복하면 됨. 카탈로그 #8 점선박스는 별도 양식이라 그대로 유지. sln 빌드 0/0. ★ 한/글 종료로 변환 실측 미완 — 사용자 확인 필요.
- 2026-07-16 +09:00 | Claude Code | **배치 STAGE 2 가 결론박스(=>) 안 문단을 건너뛰던 버그 수정 — 박스 셀만 선별 순회**. 사용자 보고: "마크다운 일괄생성 시 표 안에서 Q 가 실행되지 않는다, 셀별로 순환하며 문단마다 Q 를 먹여야 할 듯". **원인**: `HwpxWriter.StageTwoCombined` 가 `MoveDocBegin`~`MoveDocEnd` 로 **본문(list 0)만 순회** — 한/글에서 표의 각 셀은 별도 list 라 안 잡힘(주석에도 "본문(list 0) 만 순회" 명시). **★ 범위 정정**: 1차 구현은 모든 셀을 돌았으나 사용자 정정 — "원래 의도는 `=>` 로 생성되는 박스가 대상, **GFM 표는 가운데정렬(Forge 설정)이라 타겟 아님**". 정렬 기반 필터는 사용자가 `:---` 등 명시 GFM align 을 쓰면 새는 구멍이 있어 채택하지 않고, **생성 시점 식별자** 방식으로 확정. **구현**: (1) `ConclusionRenderer.Render` 반환형 void→int — `InsertText` 직후(캐럿이 아직 박스 셀 안일 때) `Range.GetCaretPos().List` 캡쳐해 반환. (2) `HwpxWriter` 가 `List<int> boxCellLists` 를 STAGE 1 dispatcher(`DispatchNodes`→`DispatchOne`)에 주입해 결론박스 셀 id 수집 → STAGE 2 로 전달. (3) `Range.ApplyOverLists(hwp, fn, listIds, ...)` 신설 — 지정 list 만 순회(내용/정렬 추측 없음). (4) `HwpxWriter.CombinedParaAction` 추출 — (들·자·들) 3단계를 본문 순회와 박스 셀 순회가 공유하는 SSOT(기존엔 StageTwoCombined 안 인라인). (5) 배치의 `isCancelled` 를 박스 셀 순회까지 전달. **실측 검증**(probe convert, 표+결론박스 md): 전체 list 11개 중 **박스 셀 1개(list#11=결론박스)만 처리**, GFM 표 8칸(#3~#10)·메타헤더(#2) 무변경 확인. sln 빌드 0/0. ★ 시각(정렬 결과) 사용자 확인 대기. 참고박스/붙임박스도 원하면 같은 방식으로 등록 가능(현재 미등록).
- 2026-07-15 +09:00 | Claude Code | **build.cmd 신설 — 버전 bump + FDD publish + zip 원스텝 자동화**. 사용자 요청: "사소한 빌드처리까지 AI 쓸 필요 없다, docmine 의 build.cmd 처럼 만들 것". .NET 은 `npm run` 등가물이 없고 기존엔 VS Code 태스크(publish:fdd:zip) 또는 수동 `dotnet publish` + `zip-publish.ps1` 뿐이었으며 **버전 bump 자동화가 없어 매번 손으로 수정**했음(빠진 조각). **신설**: (1) [.vscode/bump-version.ps1](.vscode/bump-version.ps1) — `Directory.Build.props` 의 `<Version>` 패치 +1. XML 라운드트립(`[xml].Save`)은 주석·들여쓰기를 흔들어 **정규식 치환** 사용, props 가 UTF-8(BOM 없음)+한글 주석이라 `[System.IO.File]::WriteAllText` + `UTF8Encoding($false)` 로 되씀(PS5.1 `Set-Content` 는 ANSI 라 주석 깨짐). (2) [build.cmd](build.cmd) — docmine 패턴(goto 라벨 서브커맨드) 그대로: `dev`/`build`/`test`/`publish`/`zip`/`bump`/`release`. `release` = bump+publish+zip 원스텝. `do_publish` 가 `taskkill /F /IM Forge.exe` 선행(실행 중이면 산출물 잠겨 publish 실패하던 문제 자동 처리). **★ cp949 + CRLF 로 저장 필수**(cmd 배치 파서 제약, docmine 주석과 동일) — em-dash(U+2014)는 cp949 미지원이라 배제. (3) `.gitignore` 에 `!.vscode/bump-version.ps1` 예외 추가(`.vscode/*` 무시라 build.cmd 가 다른 PC 에서 깨짐 방지) + `ref/` ignore 추가(공개 repo 에 내부 예시 .hwp 원본 미커밋, 추출 사양만 코드/로그에 보존). **검증**: `build release` 실측 — 0.5.6→0.5.7 bump + publish/Forge-v0.5.7.zip(589KB) 생성 확인, 한글 출력 정상.
- 2026-07-13 +09:00 | Claude Code | **양식삽입에 붙임/참고 라벨박스 2종 추가 + 마크다운 기본 붙임 색 교체 (정예병 예시 ref/1.hwp 실측)**. 사용자 제공 참조 파일(ref/1.hwp, tool2 아님·실제 예시)의 붙임1(파랑)·참고1(초록) 박스를 COM 으로 직접 읽어 재현. **실측 도구 신설**: `Forge.Probe tabledump` — 문서 모든 list(셀) 순회하며 셀 텍스트·CharShape(폰트/크기/색)·CellShape·CellBorderFill 덤프. **★ 셀 배경색 읽기 = MCP 권위로 경로 확정**(추측 실패 후): CellFill(id33, PS=CellBorderFill) → CellBorderFill(11)=**SelCellsBorderFill/AllCellsBorderFill/TableBorderFill** → BorderFill(5).**FillAttr**(DrawFillAttr) → DrawFillAttr(32).**WinBrushFaceColor**(0x00BBGGRR)·Type(1=단색). 셀블록 선택(TableCellBlock, SelectionMode=3) 후 fresh CreateAction 으로 읽어야 반영. 두 박스 색 저장 위치 상이(붙임1=SelCells, 참고1=AllCells)라 3곳 모두 조회. **실측값**: 붙임1 라벨=#333399(51,51,153) 파랑·HY헤드라인M 15pt 흰색, 본문 15pt; 참고1 라벨=#699B37(105,155,55) 초록·16pt 흰색, 본문 16pt; 행높이 9.63mm; 3셀[라벨/gap/본문]. **구현**: (1) `ForgeTemplates.붙임_파랑`/`참고_초록` + 공용 `라벨박스_예시` 빌더(금감원페이지참고 패턴, 본문 4변 solid 얇은 테두리). (2) 카탈로그 "붙임박스" 그룹 #21·#22 추가(#13 아래). (3) 마크다운 기본 붙임: `ReportSpec.AttachHeaderBgRgb` 0,0,255→**51,51,153**(#333399), AttachmentRenderer 가 사용·폰트/크기 이미 HY헤드라인M 15pt 일치. sln 빌드 0/0. ★ 양식삽입 #21·#22 + 마크다운 붙임 변환 시각 실측 필요.
- 2026-07-12 +09:00 | Claude Code | **W(어절 끌어올림) ESC 즉시 취소 추가**. 사용자 보고: W 가 대부분 잘 되나 가끔 비정상 진행하며 자간을 min(floor)까지 과도하게 좁힘 → "ESC 누르면 언제든 취소" 요청. W 는 UI 스레드를 점유하는 동기 루프(`Squeeze.FitCurrentParagraphToOneLine`, 최대 60 iter × SpacingDecrease)라 일반 WinForms KeyDown 이 안 뜸 → **GetAsyncKeyState(ESC) 폴링**이 정석. 계층 분리(Forge.Core 는 Forge.Win32 미참조) 유지 위해 **취소 델리게이트(`Func<bool>? shouldAbort`) 주입** 방식: (1) `NativeMethods` 에 `GetAsyncKeyState`+`VK_ESCAPE` 추가, (2) `Forge.Win32.KeyState.IsEscPressed()` 헬퍼 신설(전역 물리 키 상태 — 한/글 창에서 ESC 눌러도 감지), (3) `FitCurrentParagraphToOneLine` 에 `shouldAbort` 파라미터 추가 — SpacingDecrease 루프 매 iter 앞에서 검사, true 면 즉시 return(부분 자간 감소는 유지, 완전 초기화는 Ctrl+Shift+Z), (4) `RunWordPull` 이 `Func<bool> escAbort = KeyState.IsEscPressed` 전달(hwp 가 dynamic 이라 호출이 런타임 디스패치 → 메서드 그룹 직접 전달은 CS1976, Func 변수로 변환). sln 빌드 0/0. ★ ESC 취소 재실측 필요.
- 2026-07-12 +09:00 | Claude Code | **Q(자동정렬) ESC 취소 확장**. W 에 이어 사용자 요청으로 Q 에도 동일 ESC 취소 적용. Q 의 문단 순회 루프는 `Range.cs` 체인(`ApplyPerParagraph`/`ApplyPerParagraphInRange` → `ApplyWithinList`/`ApplyAcrossLists`/`ApplyOverCells` → `ProcessOneList`)에 걸쳐 있어, `Func<bool>? shouldAbort` 를 체인 전체에 관통시키고(내부 호출 전부 전달) 각 while/for 루프 매 반복 앞에서 `Aborted(shouldAbort)` 검사 → true 면 로그 남기고 break/return. 단일 캐럿 분기도 fn 호출 전 검사. 헬퍼 `Range.Aborted(Func<bool>?)` 신설. `RunAutoAlign` 이 `escAbort=KeyState.IsEscPressed` 를 두 진입점(InRange/기본)에 전달(dynamic 호출이라 Func 변수 경유). 배치 모드 호출자(IndentAlign/Kerning 435/230)는 3-인자 그대로 → 기본 null(취소 없음, 무영향). 취소 입도는 문단 단위(한 문단 처리는 짧아 runaway 아님) — ESC 누르면 다음 문단 진입 전 중단. sln 빌드 0/0. ★ 재실측 필요.
- 2026-07-12 +09:00 | Claude Code | **상용구 무매치 시 한/글 hang 수정 (뒤 방향 fallback 제거 + 빈 선택 GetTextFile 회피) + 상용구 탭 그리드 폭 축소**. (1) **hang 버그**: 등록 안 된 글자에서 Ctrl+Shift+C → 한/글 응답없음. 로그상 앞/뒤 두 방향 읽기 완료 후 "매치 없음" 직전 멈춤. 원인: **뒤 방향(MoveSelNextChar) fallback 이 문서 끝에서 빈 선택('')에 `BlockChar`→`GetTextFile("UNICODE","saveblock")` 를 호출** → 한/글 2018(=PIA cast 불가라 GetTextFile 경로 상시 사용)이 빈 블록 저장에서 보안/블록 모달로 빠져 hang. 이 fallback 은 방향키 확정 도입(캐럿을 글자 뒤로 보냄)으로 이미 불필요. **수정**(`GlossaryExpand`): 뒤 방향 제거(`TryMatchDirection`→`TryMatchBefore`, 앞 방향만) + **선택이 실제 잡혔을 때만 BlockChar 호출**(`Range.SelectionRange` null 이면 skip — 빈 선택에 GetTextFile 자체를 안 부름) + 무매치 정리의 `Run("Cancel")` 도 try/catch. (2) **상용구 탭 폭**: 한 글자 항목에 열이 탭 전체로 늘어나 휑함 → `AutoSizeColumnsMode Fill→None`, 그리드 `Dock Fill→Left`+`Width=430`, 준말 130/본말 240(본말은 긴 상용구 대비 넉넉히), RowHeadersWidth 30 고정. sln 빌드 0/0. ★ 무매치 hang 해소 재실측 필요.
- 2026-07-12 +09:00 | Claude Code | **상용구 로드 메모리 캐시 + 방향키 방식 사용자 실측 통과**. 앞 항목의 방향키 주입 방식 사용자 실측 "아주 잘 됨"(ㅁ→□ 정상·깜박임 0·속도 개선). 후속 정리: (1) `Glossary.Load()` 가 매 Ctrl+Shift+C 마다 settings.json 디스크 읽기+JSON 파싱하던 낭비 제거 — `_cache` 정적 필드 도입(첫 Load 시 디스크→캐시, 이후 캐시 복사본 반환). `Save`/`ResetToDefaults` 가 유일한 glossary writer 라 Save 시 `_cache=valid` 로 즉시 갱신 → 항상 최신. 반환은 방어적 복사(호출부 변경이 캐시 오염 방지), 접근 전부 UI 스레드라 lock 불필요. (2) `Sleep(30)` 은 **유지** — keybd_event 비동기라 주입 키가 한/글 큐에서 처리되기 전 COM 읽기가 앞서는 크로스프로세스 레이스 가드(cruft 아님, 30ms 체감 불가). (3) `Forge.Win32.csproj` `UseWindowsForms` 는 GlobalHotkeyManager 의 Control.BeginInvoke 가 써서 유지. sln 빌드 0/0.
- 2026-07-12 +09:00 | Claude Code | **상용구 IME 확정 — 방향키 주입으로 전면 단순화 (포커스 넛지·IMM·sink 창 전부 제거)**. 사용자 지적: 포커스 넛지 방식이 "너무 어렵게 돌아간다" — 깜박임(수정 후에도 잔여 무게)·크로스프로세스 지연으로 C 가 다른 단축키보다 체감상 매우 느림. **사용자 제안 채택**: 단축키 발동 시 **오른쪽 방향키 1회 주입** → IME 조합이 표준 동작으로 확정 + 캐럿이 확정 글자 뒤로 이동 → 기존 "앞 글자 읽기"(MoveSelPrevChar) 방향이 자연히 맞음. **구현**: `NativeMethods` 에서 IME/포커스 넛지 P/Invoke(AttachThreadInput·GetFocus·SetFocus·GetForegroundWindow·GetWindowThreadProcessId·ImmGetContext/ReleaseContext/NotifyIME·GetGUIThreadInfo·GUITHREADINFO·RECT·NI_COMPOSITIONSTR·CPS_COMPLETE) 전부 삭제, `keybd_event`(VK_RIGHT) 단일 추가(INPUT 유니언 마샬링 함정 회피). `ImeHelper` 를 `CommitComposition()` = 오른쪽 방향키 down/up 주입 한 줄로 재작성(전용 sink Form 도 폐기 — 포커스/창 조작 0 → 깜박임 0). `RealtimeTab.RunGlossaryExpand` 호출부 갱신(Sleep(30) 은 주입 키 처리 여유로 유지, 튜닝 여지). sln 빌드 0/0. ★ 재실측 필요 — ㅁ→□ 정상 + 깜박임 없음 + 속도 개선 사용자 확인 대기.
- 2026-07-12 +09:00 | Claude Code | **상용구 IME 포커스 넛지 — 메인 폼 화면 깜박임 수정 (전용 sink 창 도입)**. 사용자 실측(데스크탑 Claude 세션): ㅁ→□ 치환 자체는 정상 동작하나 "Forge 창이 갑자기 포커스 받았다 사라짐" — 다른 단축키보다 느리고 UX 이질적. **원인**: `ImeHelper.TryFocusNudge` 가 `SetFocus(ownWindow)` 의 `ownWindow` 로 **Forge 메인 폼(가시 창)** 핸들을 사용 — Win32 `SetFocus` 공식 동작이 "대상 창 또는 그 최상위 부모를 활성화" 라, 가시 창을 sink 로 쓰면 실제로 화면에 뜨는 게 정상 동작이었음 (직전 로그의 "포그라운드/z-order 불변이라 깜박임 없음" 가정은 틀렸음 — 실측 전 가정 오류). **수정**: `ImeHelper` 에 **전용 sink `Form`** 신설 — `ShowInTaskbar=false`, `FormBorderStyle=None`, 화면 밖(-32000,-32000) 0×0, **Show() 호출 안 함**(핸들만 `.Handle` getter 로 강제 생성). `CommitForegroundComposition()` 시그니처에서 `ownWindow` 파라미터 제거(내부 sink 사용), `RealtimeTab.RunGlossaryExpand` 호출부도 `Main.Handle` 전달 제거. 안 보이는 sink 는 활성화돼도 그릴 게 없어 화면 변화 없음. sln 빌드 0/0. ★ 재실측 필요 — 사용자 확인 대기.
- 2026-07-10 +09:00 | Claude Code | **상용구 IME 확정 — 포커스 넛지 방식으로 교체 (ImmNotifyIME 미반응)**. 앞 항목의 `ImmNotifyIME(CPS_COMPLETE)` 는 한/글 2018 에 무효(원복 지속 실측) — 한/글이 표준 IMM 아닌 TSF 계열 사용 추정. 사용자가 확인한 "창 전환하면 조합 확정" 을 재현: `ImeHelper.CommitForegroundComposition(ownWindow)` 에 **포커스 넛지** 추가 — `AttachThreadInput(uiThread, 한글thread)` 후 편집창(GetFocus) 포커스를 우리 창(Main.Handle)으로 잠깐 옮겼다(SetFocus) 되돌림 → 편집창 WM_KILLFOCUS 로 조합 확정. 포그라운드/z-order 불변이라 화면 깜박임·캐럿 이동 없음. hotkey 콜백은 BeginInvoke 로 UI 스레드 실행이라 스레드 일치 확인됨. IMM 시도는 무해하니 1차로 남기고 넛지를 2차로. 또 매치 루프를 **준말 길이별 1회 읽기**로 최적화(BlockChar 호출 최소화, 속도). sln 빌드 0/0. ★ 실제 키보드 조합으로만 검증 가능 — 사용자 확인 필요.
- 2026-07-10 +09:00 | Claude Code | **상용구 — IME 조합 확정 전처리 (원복·방향꼬임 근본 수정)**. 증상: (1) ㅁ→□ 변환 후 커서 이동 시 원복, (2) 앞칸을 헛선택 후 두 번째(뒤 방향)에 동작. 원인: 전역 단축키는 포커스를 안 뺏어 한/글 IME 조합(ㅁ)이 미확정 상태 → COM 편집(□)이 조합 버퍼에 덮여 커서 이동 시 되살아나고, 캐럿도 준말 앞에 남아 backward 가 헛돎. **수정**: `Forge.Win32.ImeHelper.CommitForegroundComposition()` 신설 — 포그라운드 스레드의 포커스 창(GetGUIThreadInfo.hwndFocus)에 `ImmNotifyIME(NI_COMPOSITIONSTR, CPS_COMPLETE)` 로 조합 확정. `RunGlossaryExpand` 진입 시 호출 + 크로스프로세스 반영 위해 Sleep(30). 확정 후 캐럿이 준말 뒤로 와 앞(MoveSelPrevChar) 방향이 바로 맞음(양방향은 안전망 유지). **COM 메커니즘 실측**(`Forge.Probe glosstest`): 확정 입력 기준 5/5 정상(`.`·`ㅈ`·`>`·`ㅁ`·`ㅇ` 전부 앞 방향 즉시 치환). IME 조합 경로는 실제 키보드로만 검증 가능. sln 빌드 0/0.
- 2026-07-10 +09:00 | Claude Code | **상용구 확장 — 캐럿 앞/뒤 양방향 매치 (IME 조합 대응)**. 진단(`Forge.Probe glossary`): 조합 중(blink) 자모는 **범위선택 아님**(SelectionRange=null, SelectionMode=0)이고 `GetPosBySet`가 엉뚱한 Pos(문단 3글자인데 19) 반환. 단, 전역 단축키 발동(창 전환)이 조합을 확정시켜 캐럿이 준말 '뒤'로 떨어지는 게 보통 → 기존 `MoveSelPrevChar`(앞) 로직이 대체로 맞음. 환경 편차 대비로 **앞(MoveSelPrevChar) 먼저, 안 되면 뒤(MoveSelNextChar)** 를 시도하도록 `TryMatchDirection` 헬퍼로 양방향화(`GlossaryExpand`). Probe 에 `glossary`/`glossx` 진단 서브커맨드 추가(한/글 2018=PIA cast 불가라 텍스트는 GetTextFile fallback). sln 빌드 0/0.
- 2026-07-08 +09:00 | Claude Code | **상용구 확장에 선택-영역 모드 추가**. `GlossaryExpand.ExpandAtCaret` — 선택 영역이 있으면(`Range.SelectionRange` non-null) 캐럿 앞이 아니라 **선택된 텍스트**를 `BlockChar`로 읽어 등록 준말과 정확 일치 시 Delete+InsertText 치환(no-op if 불일치). 선택 없으면 기존 캐럿-앞 모드 그대로. 예: 'ㅁ' 선택 상태에서 Ctrl+Shift+C → '□'. sln 빌드 0/0.
- 2026-07-08 +09:00 | Claude Code | **상용구(glossary) 기능 신설: Ctrl+Shift+C 확장 + 관리 탭 + 장평 floor 90→50**. (1) **상용구 확장(C)**: 한/글에서 준말 입력 후 캐럿 바로 뒤에서 Ctrl+Shift+C → 준말을 본말로 치환. `GlossaryExpand.ExpandAtCaret` — before 길이 내림차순 검사(긴 준말 우선), 각 항목마다 원위치→`MoveSelPrevChar`×len→`IndentAlign.BlockChar`(InitScan/GetText, 선택 보존)로 읽어 일치 시 `Run("Delete")`+`ComHelpers.InsertText(after)`. **MCP 확인**: `MoveSelPrevChar`(id 498 셀렉션 이전글자), `Delete`(선택영역 삭제 — HwpxWriter 관례). (2) **상용구 탭 신설**(`GlossaryTab`, MainForm 5번째 탭): DataGridView 로 변경전(준말)/변경후(본말) 편집, 행 추가·삭제·기본값 복원, 편집 즉시 자동 저장. `Glossary`(Forge.Core) — 모델(`GlossaryEntry`)+settings "glossary" 섹션 영속화+기본 5종(`.`→·, ㅈ→§, `>`→→, ㅁ→□, ㅇ→◦). `Actions.All` 끝에 `glossary_expand`(hkId 13, 키 C) 추가, RealtimeTab 행 12 + 핸들러. `MdlIcon.Add`(U+E710)·`Delete`(U+E74D) 추가. (3) **장평 floor 수정**: 사용자 피드백 "90 밑으로 안 내려감" → `CharRatioFloor` 기본 90→**50**(HWP 하한), 이제 100→95→…→50. CLAUDE.md §2·§3.4 (5탭·신규 파일)·단축키표(E·C), MainForm 주석 갱신. sln 빌드 0 경고 0 오류, 테스트 16/16. ★ Forge.UI 재빌드·재시작 후 반영, 런타임(치환·CharShape) 실측 미검증.
- 2026-07-08 +09:00 | Claude Code | **장평 단축키 동작 변경(E: 누적 감소) + Z 에 장평 초기화 추가**. (1) **E 재설계**: 고정값 설정 → **누를 때마다 현재 문단 장평 5%씩 감소, 최소 floor 에서 멈춤**. 현재 장평은 `Primitives.GetCharWidthRatio`(CharShape get — MCP id=8: selection 없으면 캐럿 위치 단일값, `MoveParaBegin` 후 읽음)로 읽어 -5, floor clamp 후 문단 전체 적용. UI 값칸은 이제 **최소 장평(floor)** 의미(필드 `CharWidthRatio`→`CharRatioFloor`, key `char_width_ratio`→`char_ratio_floor`, 기본 95→**90**, 스텝 `CharRatioStep=5` 고정), 버튼 "현재 문단 장평 −5%". (2) **Z(자간 0 초기화) 확장**: `RunKerningReset` 양 분기(선택/현재문단)에 `SetCharWidthRatio(hwp,100)` 추가 → **자간 0 + 장평 100% 동시 초기화**, 버튼 "자간·장평 초기화". CLAUDE.md §2 단축키표(E·Z) 갱신. sln 빌드 0 경고 0 오류. ★ Forge.UI 재빌드·재시작 후 반영, 런타임 실측 미검증.
- 2026-07-08 +09:00 | Claude Code | **단축키 추가: Ctrl+Shift+E — 현재 문단 장평 설정(기본 95%)**. 현재 문단 전체 글자 장평(가로 비율)을 UI 입력 값으로 일괄 설정. **MCP 권위**: tool2 `글자장평`(디컴파일 line 116) — `CharShape` 의 `Ratio{Hangul,Latin,Hanja,Japanese,Other,Symbol,User}` 7개 일괄 SetItem, 값 50~200%(PIT_UI1). **구현**: (1) `Primitives.SetCharWidthRatio(hwp, ratioPct)` — tool2 글자장평 등가, 50~200 clamp. (2) `RealtimeTab.RunCharWidthRatio` — RunParagraphSize8 과 동일 패턴(MoveParaBegin→MoveSelParaEnd→적용→Cancel, 현재 문단). (3) `CharWidthRatio`(기본 95) 상태 + 영속화(`char_width_ratio`) + UI 값칸(`BuildCharRatioCluster`, % 라벨) + LoadFromSettings/OnResetSettings. (4) `Actions.All` 끝에 `char_width_ratio`(hkId 12, 기본키 E) 추가 — Id 안정성 위해 끝에만 추가 규칙 준수. (5) BuildMainGroup 행 6 신설(para_size D 의 쌍둥이), 이후 행 renumber, GroupBox Height 640→690. CLAUDE.md §2 단축키표에 Ctrl+Shift+E 반영. sln 빌드 0 경고 0 오류, 테스트 16/16. ★ Forge.UI 재빌드·재시작 후 단축키 등록됨(letter 는 탭① 에서 변경 가능).
- 2026-07-07 +09:00 | Claude Code | **Q 자동정렬 — 표 셀 블록 선택 미동작 수정 (셀 블록 감지 + 셀별 문단 순회)**. 증상: 표 안에서 (1) 단일 셀 내 여러 문단을 **텍스트 드래그** 선택하면 잘 순회하나, (2) **여러 셀** 선택 시 미동작, (3) **단일 셀** 선택 시 첫 문단만 처리. 원인: 셀 블록은 `GetSelectedPosBySet` 가 `start==end` 로 접히거나 list 만 다르게 와 신뢰 불가 → `Range.SelectionRange` 의 `start==end→null` 가드에 걸려 `ApplyPerParagraph` 가 **단일 캐럿 모드**로 오인 → 첫 문단만 처리. **MCP 권위 확인**: tool2 `셀정보`(디컴파일 line 1137)·`블록리스트생성`(line 1179)이 셀 블록엔 `GetSelectedPos` 대신 **`CellShape`(표 밖 접근 시 예외 → 표 감지자) + `InitScan(scanWithinSelection)`/`GetText`(2·3=내용 있음) + `MovePos(201=moveScanPos)`(블록 시작 이동)** 사용, 표 셀 list-id 는 **행 우선 연속 정수**. `SelectionMode`(&0x0F, 0=선택없음)로 bare caret 배제. `KeyIndicator` 는 out 파라미터라 C# dynamic 부적합 → 미사용(기존 방침 유지). **구현**(`Linter/Range.cs`): (1) `TryGetCellBlockRange` — CellShape로 표 감지 → 순수 텍스트 selection(같은 list, start≠end)은 null(기존 within-list 유지, 증상1 보존) → 다중 list면 (min,max) → 그 외 SelectionMode≠0 & scan으로 블록 확인 시 (scan시작셀 list, 캐럿 list)의 (min,max). (2) `ApplyOverCells`+`ProcessOneList`(공용) — 경계 셀 list 사이를 훑어 각 셀의 모든 문단 순회(멱등 정렬이라 사이 낀 비선택 셀 무해). (3) `ApplyPerParagraph` 진입 시 셀 블록 우선 분기, `ApplyAcrossLists` 는 `ProcessOneList` 로 리팩터. 표 밖 케이스는 CellShape 예외 즉시 null → 100% 무변경. sln 빌드 0 경고 0 오류, 테스트 16/16 통과. ★ **런타임(한/글 COM) 실측 미검증** — Forge.UI 재빌드·재시작 후 셀 블록 Q 로 사용자 확인 필요(로그에 `[cell]` 결정 과정 출력).
- 2026-06-15 +09:00 | Claude Code | **GFM 표 X 변환 실패 수정(구분행 하이픈 1개 이상 허용) + Q/W 마커 대량 추가**. (1) **표 버그**: `| -- | ---- |` 처럼 구분행 첫 셀 하이픈이 2개인 GFM 표를 Ctrl+Shift+X(선택→md)로 변환 시 표가 안 만들어지고 텍스트로 처리됨. 원인: `Parser.TableSepRegex` 가 셀당 `-{3,}`(3개 이상) 요구 — prettier 등 포매터가 헤더 폭에 맞춰 `--` 로 줄인 표를 인식 못 함. GFM 표준은 셀당 하이픈 1개 이상 허용. **수정: `-{3,}` → `-+`** (SplitRow/ParseAligns 는 `|` split+trim 기반이라 하이픈 개수 무관, 정규식만 문제였음). **실측 검증**: 한/글 추출 텍스트(2574자·28줄, `|`=U+007C 정상) → Parser.Parse → Table 1노드 → mdconv 변환 후 tbl 컨트롤 0→1 (표 생성 OK). (2) **마커 추가** (`IndentAlign.BulletMarkers` — Q 자동정렬·W 어절끌어올림 공용 SSOT): 화살표/강조 `⇨ ◈ ➡ → ☞ ⇒ ▣ ▷ ▶ ◉`, 원문자 `①~⑮`, 굵은원 `❶~⓯`, 이중원 `⓵~⓾`(유니코드 1~10 한계), 네모 PUA(한컴 HFT — `U+F02B0~F02B9`·`U+F02CD~F02D6`, scan 실측 — 표준 유니코드에 네모숫자 없음 확인). (3) **Forge.Probe 진단 커맨드 신설**(영구 도구): `scan`(현재 selection 의 컨트롤·앵커·추출텍스트·줄분해·Parser결과·codepoint 덤프, 비파괴), `mdconv`(실제 X 변환 + tbl 보존 정량 검증), `parse <file>`(md 파일 파싱 노드 덤프). Core 빌드 0 경고 0 오류. ★ 새 동작(표 인식·마커)은 Forge.UI 재빌드·재시작 후 GUI 단축키에 반영됨.
- 2026-06-12 +09:00 | Claude Code | **Ctrl+Shift+X 선택영역 md 변환 — 표 포함 영역에서 표 손실 버그 수정 (표 보존 + 구간 분할 변환)**. 증상: 표가 든 영역을 드래그해 X 를 쓰면 표가 사라짐. 원인: `ConvertSelectionToHwpx` 가 ① 선택 텍스트 추출 → ② `Run("Delete")` 로 선택 영역 통째 삭제 → ③ 텍스트 md 변환 구조라, plain text 가 표 구조를 복원 못 하는데 Delete 는 표를 통째로 지워 영구 손실. **사용자 실측 진단(한/글 2018, Forge.Probe `scan`/`mdconv` 신설)**: ① PIA(IHwpObject) cast 실패 환경이라 `GetSelectionText` 가 InitScan/GetText 가 아닌 `GetTextFile("saveblock")` fallback 사용 → GetText state 4 기반 감지 무효. ② '개체'(floating, TreatAsChar=0) 표는 `GetTextFile` 추출에서 아예 누락(한컴디벨로퍼 공식 답변과 일치 — '글자처럼 취급'만 추출됨) → 텍스트 기반 감지 불가능. ③ 해결책: **HeadCtrl~Next 컨트롤 순회 + `GetAnchorPos`(조판부호 위치)** 로 표 감지 — PIA 무관 순수 dynamic dispatch, inline/floating 표 모두 본문 list 에 조판부호가 있어 동일하게 잡힘. **MCP 권위**: `CtrlCh`(id=354)==11 = "그리기 개체/표" (secd/cold 구조 컨트롤은 ch=2 제외), `CtrlID`(id=355) tbl=표, `GetAnchorPos`(id=361) → ListParaPos, `HeadCtrl`(id=9)/`Next`(id=357) linked-list, `MoveSelNextParaBegin`(id=491). **구현**: (1) `Range.CollectInlineObjectParas` — selection 범위 내 CtrlCh==11 개체의 앵커 문단 번호 목록(앵커가 본문 list·범위 내인 것만). `SelectionContainsInlineObject` 는 wrapper. (2) `Range.SelectParaRange` — MoveParaBegin→MoveSelNextParaBegin×N→MoveSelParaEnd 로 문단 구간 블록 선택. (3) `HwpxWriter.ConvertSelectionToHwpx` 재작성 — 표 문단을 경계로 selection 을 텍스트 구간으로 쪼개, **표 문단은 건드리지 않고** 각 구간만 **역순(아래→위, 문단번호 보존)** 으로 [선택→추출→Delete→cursor 변환]. 빈 구간(표 사이 빈줄)은 Cancel 로 보존. 표 없으면 기존 전체 변환(`ConvertSelectionWhole`). (4) `Primitives.GetSelectionText` 에 `out bool containsObject`(state 4) 오버로드 — PIA 가능 환경 보조 감지로 유지. **실측 검증 2회**: 변환 전후 tbl 컨트롤 1→1 (표 보존 OK), 표 위·아래 텍스트는 정상 md 변환. Core 빌드 0 경고 0 오류. (단, 새 동작은 Forge.UI 재빌드·재시작 후 GUI 의 X 단축키에 반영됨.)
- 2026-05-20 +09:00 | Claude Code | **W5 — UI 시각 통일 + ForgeTemplates 11종 + 아이콘 + 단일파일 publish 검증**. (1) **ForgeTheme 도입** (`src/Forge.UI/ForgeTheme.cs`): 색 팔레트(Accent #3E57A5, Background #F8F9FB, Success/Warning/Error 등) + Segoe UI/Consolas 폰트 토큰 + Pad/PadLg 패딩 상수 + StyleFlatButton/StyleInput/StyleGroup/SectionTitle/SectionDesc 헬퍼. 모든 폼·탭·컨트롤이 단일 design token 으로 일관. (2) **MainForm 재설계**: 4 탭(⓪ How to ① 실시간 ② 양식 ③ 마크다운) + 상단 status bar(● dot 회색→녹색 attach 시) + 한/글 선택 + ? About + 하단 footer(버전 라벨) + 시작 탭 = ①. AboutForm 별도 클래스(Forge 타이틀 + 버전 + 설명 + kinphw github). `ForgeVersion.Version = "0.4.0"`. (3) **HowToTab 신설**(`Tabs/HowToTab.cs`): Python howto_tab.py 1:1 — 3탭 구성 + 11개 md 문법 GroupBox key|desc grid. (4) **MarkdownTab/RealtimeTab/TemplatesTab 전면 리뉴얼**: 모두 ForgeTheme 적용. MarkdownTab TableLayoutPanel 50:50(좌 입력 · 우 로그). RealtimeTab 3컬럼 grid([버튼 230 | 폰트 290 | 단축키 %]) × 12행 + 그룹 separator + UserSettings persist (500ms debounce). TemplatesTab 카탈로그형 — 그룹 separator(헤더/참고박스/붙임박스/기호) + 각 항목 [버튼 + 설명 label] + 하단 로그. (5) **★ ForgeTemplates 11종 신설**(`src/Forge.Core/Templates/ForgeTemplates.cs`, 이름 의도: tool2/FSS 가 아니라 **forge 가 tool2 권위에서 큐레이션한 forge 전용 카탈로그**): 메타헤더(제목+팀+일자) / 중제목 / 소제목 / 꺽쇠박스 / 점선박스 / 참고박스_마크다운 / 블루진박스 / 금감원페이지참고 / 당구장 / 십자가 / 꺽쇠_홑·겹. tool2 행안부초록표 권위 패턴 1:1. (6) **★ C# dynamic + COM optional args 함정 박제**: `hwp.MovePos(106)` 1-인자 호출이 `TargetParameterCountException: Error while invoking MovePos` 로 실패 — Python pywin32 의 typelib early-bound 는 default 자동 채움 vs C# dynamic 은 actual arg count 매칭 strict. **해결: 모든 MovePos 호출을 3-인자 명시 — `hwp.MovePos(106, 0, 0)`**. 4 위치(꺽쇠박스 2 + 블루진박스 2) 일괄 수정. **참고**: 한컴 공식 developer forum bhjung 답변(Hancom employee)도 "MovePos 함수는 부정확함, Goto action 으로 우회 권고" — 현 fix 는 경험상 동작하나 fragile. 사용자 GUI 실측 11/11 OK. (7) **★ WinForms layout 함정 박제**: ① **FlowLayoutPanel + AutoSize 자식 width 미상속** — TableLayoutPanel 으로 교체(SplitContainer 도 SplitterDistance 타이밍 문제로 동일 교체). ② **GroupBox AutoSize 타이밍** — `AutoSize=false + Height 명시 + MinimumSize` 패턴이 안정. (8) **ForgeIcon 신설**(`src/Forge.UI/ForgeIcon.cs`): 64×64 Bitmap 에 "F" 글리프 3 사각형 합성(slate-800 배경 + amber-500 foreground) + `Bitmap.GetHicon` → `Icon.FromHandle` clone + `DestroyIcon` cleanup. Python icon.py 1:1, 외부 .ico 의존 0. MainForm 생성자에서 try/catch (아이콘 실패는 치명적 아님). (9) **★ 단일파일 publish 검증** — `dotnet publish -c Release -r win-x64 -p:PublishSingleFile=true -p:SelfContained=false -p:IncludeNativeLibrariesForSelfExtract=true`: **단일 `Forge.exe` 807KB** (5MB 목표 대비 6배 여유). self-contained 동일 옵션 + `EnableCompressionInSingleFile=true` 는 69MB — .NET 8 Desktop Runtime 미설치 PC 대상 fallback 용. PDB는 `<DebugType>embedded</DebugType>`(Directory.Build.props) 로 dll/exe 내장 → publish 디렉토리에 .pdb leak 0. csproj 에 `AssemblyName=Forge` + `Version/FileVersion/AssemblyVersion=0.4.0(.0)` + `Product/Company/Copyright` 메타. (10) **검증**: 전체 솔루션 빌드 0 경고 0 오류. 사용자 GUI 실측(4 탭 전환 / 11 템플릿 / 한/글 attach / About) 모두 OK. **다음 W6**: 사용자 운영 중 발견 이슈 대응 + 잔여 polish.
- 2026-05-19 +09:00 | Claude Code | **W4 — WinForms GUI + 9 전역 단축키 + dispatch overhead fast-skip 최적화**. (1) **포팅된 GUI 모듈** (Python forge/ui → C# src/Forge.UI, ~1,200줄): `Program.cs` (STA Main), `MainForm.cs` (1200x900 윈도우 + status bar + 3 탭 + footer + `EnsureHwp` lazy attach + About + 한/글 선택 picker), `AppState.cs` (HwpSession + ReportSpec + PreferredMoniker), `HwpPickerForm.cs` (다중 인스턴스 ListBox 다이얼로그), `Tabs/MarkdownTab.cs` (md 입력 + 작성부서/일자 + 변환 + 다른 이름 저장 + 로그 — 종듨 작동), `Tabs/RealtimeTab.cs` (9 단축키 letter Entry + 폰트 4쌍 + blank size + 룰 버튼 풀세트 + GlobalHotkeyManager 통합), `Tabs/Actions.cs` (9 ActionDef 카탈로그, Python actions.py 1:1). TemplatesTab 은 placeholder (spec 편집 fine-tune 별도). `UserSettings.cs` Forge.Core 에 추가 (`%APPDATA%\Forge\settings.json` JSON 영속화). (2) **Forge.Win32 hotkey 인프라**: `NativeMethods.cs` (RegisterHotKey/UnregisterHotKey/GetMessage/PostThreadMessage P/Invoke + MSG struct), `GlobalHotkeyManager.cs` (백그라운드 STA 스레드 메시지 펌프 + `Control.BeginInvoke` UI dispatch + dynamic Replace + Stop). Forge.Win32.csproj 에 `UseWindowsForms=true` (Control.BeginInvoke 사용). (3) **9 전역 단축키**: Ctrl+Shift+Q/W/A/S/F/G/D/Z/X — 자동정렬·어절끌어올림·본문폰트·주석폰트·헤드라인폰트·울릉도폰트·문단크기·자간0·선택영역md변환. Python ui/actions.py 의 9 액션 그대로. `UserSettings.SetKeymapEntry` 로 letter 변경 영속화. (4) **dispatch overhead 최적화**: Python 원본의 STAGE 2 후처리가 빈 문단마다 `MoveSelNextWord` 10회 + `BlockChar` 10회 시도 후에야 skip — C# dynamic 의 IDispatch 호출이 pywin32 typelib early-bound 보다 느려 시각상 부산스러움 (사용자 보고). 해결: `IndentAlign.ProcessParagraph` 와 `Kerning.AdjustParagraph` 시작에 **fast-skip** 추가 — `MoveParaBegin + MoveSelParaEnd + BlockChar` 한 번에 추출 → 빈 텍스트면 즉시 `MoveNextParaBegin`. 빈 문단당 dispatch 20회→4회. (5) **C# dynamic의 nullable struct 자동 unwrap quirk 발견** (W4 디버깅): `var sel = Linter.Range.SelectionRange(hwp)` 호출 시 hwp 가 dynamic 이라 전체 호출이 dynamic dispatch → 반환된 `Nullable<(CaretPos,CaretPos)>` 의 Nullable wrap 이 자동 unwrap 되어 `sel.HasValue` 가 `RuntimeBinderException` (ValueTuple 직접에 HasValue 없음). 해결: 호출 직전 `object hwpObj = hwp` 로 cast → dynamic dispatch 회피, 정적 binding 으로 Nullable 보존. **향후 sub-COM 호출 후 Nullable 반환 시 같은 패턴 적용**. (6) **검증**: 전체 솔루션 빌드 0 경고 0 오류. xUnit 16/16. 사용자 GUI 실측 (Forge.UI dotnet run) — 9 단축키 등록, 한/글 변환 + STAGE 2 + 저장, Ctrl+Shift+X 선택영역 md 변환, 본문 폰트 적용 모두 OK. **다음 W5**: TemplatesTab 의 spec 편집 UI + verbose 로그 정리 (STAGE 2 trace 제거) + Forge.UI 아이콘 + 패키징 (`dotnet publish -p:PublishSingleFile=true` 로 ~5MB exe).
- 2026-05-19 +09:00 | Claude Code | **W3 — Linter 4종 + STAGE 2 후처리 활성화 + 선택영역 변환 완성**. (1) **포팅된 linter 모듈** (Python forge/linter → C# src/Forge.Core/Linter, ~900줄): `Range.cs` (selection_range + apply_per_paragraph + Get/SetCaretPos via `GetPosBySet`/`SetPosBySet` — out 파라미터 회피, 한컴 공식 권고 path). `IndentAlign.cs` (tool1 super_shift_tab_text 1:1 — bullet/annotation/section 마커 판별 + body 워드 검색 + ParagraphShapeIndentAtCaret + 본문/객체 영역 순회). `Kerning.cs` (tool1 word_saver_text 1:1 — 자간 0 reset + 매 줄 ±1 ±15회 SpacingDecrease/Increase + 본문/객체 영역 순회). `Squeeze.cs` (어절 1개 끌어올림 — KeyIndicator 우회로 `LineDiff` (`MoveLineDown` 반복+caret 비교) 자작). (2) **C# dynamic 의 out 파라미터 한계 우회**: Python 의 `hwp.GetPos(out list, out para, out pos)` 와 `hwp.KeyIndicator(out 8개)` 는 C# dynamic 바인더가 처리 못 함. 우회: ① GetPos → `GetPosBySet()` ParameterSet 반환받아 `.Item("List"/"Para"/"Pos")` 추출 (한컴 공식 — HwpAutomation_2504.txt p.29 "포인터 미지원 언어 대응"). ② SetPos(l,p,pos) → `CreateSet("ListParaPos")` + 3 필드 SetItem + `SetPosBySet(pset)`. ③ KeyIndicator (line 번호 측정용) → 직접 우회 함수 — `MoveLineDown` 반복 + caret pos 변화 감지로 두 위치 간 줄 수 차이 계산. **결정적 발견**: `ListParaPos` ParameterSet 의 `.Item("Name")` getter 가 W2 의 `SetItem(name, value)` 와 같은 dispatch path 라 정상 작동 → 새 sub-COM dispatch 한계 재발 없음. (3) **HwpxWriter STAGE 2 후처리 활성화**: 들여쓰기 → 자간 → 들여쓰기 3단계 (Python 동일 순서). 1차 인덴트로 wrap 기준 고정 → 자간 보정 → 2차 인덴트로 drift 보정. `applyIndentAlign`/`applyKerning` 플래그 default true. cursor 모드는 skip (기존 문서 보호). 각 단계 try/catch — 중단되어도 변환 자체는 완료. (4) **ConvertSelectionToHwpx 구현**: selection_range 검사 → `GetTextFile("UNICODE", "saveblock")` 으로 plain text 추출 → `Run("Delete")` → `Parser.Parse(text)` → `GenerateHwpxViaCom(mode=Cursor)` 그 자리 emit. tool2 `마크다운()` 패턴 1:1. NoSelectionException 두 ctor (message, inner) 추가. **사용 시점**: 향후 W4 GUI 의 Ctrl+Shift+X 단축키 핸들러에서 호출. (5) **dynamic dispatch 호환성 보완**: ① `ParaActionFn` delegate signature 에 `dynamic` 못 씀 → `object` 받고 internal `dynamic` cast. ② method group 을 dynamic dispatch 인자로 직접 못 넘김 → `ParaActionFn action = ProcessParagraph;` 명시 delegate 변수. ③ `LogFn` 이름 충돌 (HwpxWriter vs Linter) → inline 람다 bridge. ④ `result[1]` indexer dispatch — Python tuple-like 받기 — dynamic 으로 indexer 시도 + fallback ToString(). (6) **검증**: 전체 솔루션 빌드 0 경고 0 오류. xUnit 16/16 통과 (Parser 단위 테스트 영향 없음). **STAGE 2 실측은 한/글 띄운 후 W4 GUI 검증과 함께** — Probe convert 의 콘솔 로그에 `[STAGE 2] (1/3) 들여쓰기 정렬` 등 단계 출력 확인 예정. **다음 W4**: WinForms GUI (Forge.UI) — 메인 윈도우 + 3 탭 (실시간/양식/마크다운) + 단축키 (Forge.Win32.GlobalHotkeyManager) + HwpPicker 다이얼로그. Python ui/ 패키지 ~2,500줄 포팅.
- 2026-05-19 +09:00 | Claude Code | **W2 — C# 포팅 STAGE 1 완성 (md → 새 hwpx) + 한컴 OLE dispatch 디버깅 결정적 단서 박제**. (1) **포팅된 모듈** (Python forge → C# src/Forge.Core, ~1,800줄): `Formatter/Parser.cs` (parser.py 1:1, YamlDotNet 17.1.0), `Formatter/HwpxWriter.cs` (hwpx_writer.py dispatcher 1:1, STAGE 2 linter 자리만), `Formatter/{Metadata,Node,MarkdownDocument}.cs` (records + NodeType enum), `Templates/{PageMargins,BulletStyle,TableStyle,ReportSpec}.cs` + `ReportSpec.Report1` static, `Renderers/Primitives.cs` (~30 COM 헬퍼, primitives.py 1:1), `Renderers/{Metadata,Section,Subsection,Bullet,Annotation,Conclusion,NoteCallout,Attachment,Table}Renderer.cs` (9 종, base.py + 9 렌더러 1:1). `Rgb.cs` 공용 RGB 타입 (Drawing.Color 의존 회피). xUnit 16 케이스 (ParserTests) 통과. (2) **★ 한컴 OLE dispatch 디버깅 결정적 단서 — 향후 sub-COM 호출 작성 시 참조 필수**: ① **PIA tlbimp 의 `[DispId]` attribute 는 부정확** — tlbimp 가 메서드 순서대로 자동 dispid 부여 (CreateItemArray PIA=15001 vs 실제 15000). 사용 금지. ② **PIA cast 도 fail** — 한컴 IDispatch 가 typed interface IID (IDHwpParameterSet `599cbb08-...`, IDHwpParameterArray `cae59b55-...`) 안 응답 → `E_NOINTERFACE`. ③ **`ITypeInfo.GetIDsOfNames` 도 `NotImplementedException`** — 한컴이 이 메서드만 안 만듦. ④ **`ITypeInfo.GetFuncDesc + GetNames` 는 정상 동작** — typelib 전체 함수 list + 정확한 dispid 추출 가능 (HTableCreation 22 멤버, ParameterArray 2 멤버 dump 성공). 한컴 sub-COM dispatch 의 유일한 정공법. ⑤ **`SetItem` 은 typelib 에 별도 등록 없음** — `Item` property 의 PROPERTYPUT variant 가 등가 (Python pywin32 의 `arr.SetItem(i, v)` 가 실제로 이 dispatch). DISPID_PROPERTYPUT(-3) named arg 필요. ⑥ **dynamic ComBinder 의 LPDISPATCH 반환 무시 issue** — `T.CreateItemArray("ColWidth", n)` 의 LPDISPATCH 반환을 callsite 가 `UpdateAndExecuteVoid*` 로 처리해 항상 null. Type.InvokeMember 도 동일. → CreateItemArray 반환값 무시하고 `T.ColWidth` getter 로 ParameterArray 재접근 (Python tool2 패턴). ⑦ **★★★ 진짜 root cause: `TreatAsChar = 1` 누락** — `HTableCreation.TableProperties.TreatAsChar` 가 default(0=floating) 면 표가 anchored frame 처럼 처리 → 캐럿이 셀 안 갇혀 **nested 표** 발생 + 다음 dispatch **`RPC_E_SERVERFAULT`** (한컴 modal corruption). `TreatAsChar=1` (글자처럼 취급) set 하면 표가 inline char → 캐럿 자연 흐름 → nested 없음 + corruption 없음. **한컴 디벨로퍼 사이트 nuristar 의 CreateTable 코드** 권위. ⑧ **표 탈출 공식 패턴: `Run("CloseEx") + Run("MoveLineDown")`** — 한컴 디벨로퍼 jjeong 답변 (`CloseEx` 후 아래로 한 줄). tool2 의 `escape_table (CloseEx+MoveDown+CloseEx)` 은 다행 표용. 단일셀 표는 `CloseEx + MoveLineDown` 으로 충분. ⑨ **이전 시행착오 (전부 잘못된 길)**: `MoveRight + BreakPara` (Python tool2 한컴 NEO 가정 패턴), `MoveDocEnd` / `MoveParentList` / `MoveNextPos`, `CloseEx+MoveDown+CloseEx`, Sleep 50~300ms, dispatch retry — 모두 `TreatAsChar=0` 상태에서는 nested 또는 modal corruption. **교훈: 정답은 영리한 dispatch sequence 가 아니라 `TreatAsChar=1` 한 줄.** (3) **신규 자산**: `src/Forge.Core/Interop/Interop.HwpObject.dll` (한컴 typelib `C:\Program Files (x86)\Hnc\Office 2018\HOffice100\bin\HwpObject.tlb` 의 tlbimp PIA, 진단 + 향후 typed dispatch 자산), `src/Forge.Core/TypelibDispatch.cs` (ITypeInfo.GetFuncDesc 통한 typelib dump + IDispatch.Invoke 직접 호출 helper + indexed PROPERTYPUT marshalling, sub-COM dispatch 한계 재발 시 사용), `src/Forge.Core/ComLateBind.cs` (Type.InvokeMember 래퍼). (4) **Forge.Probe 추가**: `convert <in.md> <out.hwpx>` (W2 검증), `diag` (PIA dispid vs IDispatch.GetIDsOfNames vs ITypeInfo dump 비교 — 미래 dispatch 진단). (5) **검증**: `tests/samples/w2_smoke.md` 40 노드 → 새 hwpx, 10 박스 모두 nested 없이 시각 정상 (사용자 캡쳐 확인). xUnit 16/16 통과. 전체 솔루션 0 경고 0 오류. **참조 URL**: developer.hancom.com (nuristar `CreateTable` + jjeong `CloseEx` 답변). **다음 W3**: linter 4종 (kerning/squeeze/indent_align/_range) 포팅 + STAGE 2 후처리 활성화 + `ConvertSelectionToHwpx` (선택영역→md→hwpx).
- 2026-05-19 +09:00 | Claude Code | **W1 — C# 포팅 시작 + 솔루션 스캐폴드 + Probe PoC 검증**. (1) **결정 사항**: net8.0-windows + WinForms + dynamic COM + framework-dependent single-file exe(~3-5MB). 운용 PC(LG gram 노트북) 에 `Microsoft.WindowsDesktop.App 8.0.11` 설치 확인됨 → 추가 배포 불필요. 사용자 핵심 동기 = **사이즈** (현 PyInstaller 20MB → 포팅 5MB 목표). self-contained(70MB) / .NET Framework 4.8(1-3MB but 옛 BCL) 양안 모두 검토 후 .NET 8 framework-dependent 채택. Python 코드는 `csharp-port` 브랜치에서도 그대로 보존(legacy-py 대체 안 함). (2) **스캐폴드**: `Forge.sln` + 5 프로젝트 — `src/Forge.Core`(domain, GUI 무의존), `src/Forge.Win32`(P/Invoke 격리), `src/Forge.UI`(WinForms 진입점 골격), `src/Forge.Probe`(`diagnose.py` 등가 콘솔), `tests/Forge.Core.Tests`(xUnit). `Directory.Build.props` 로 net8.0-windows + Nullable + C# latest 일괄. `.vscode/launch.json` + `tasks.json`(F5 디버그 / Ctrl+Shift+B 빌드). `.gitignore` 갱신(.NET bin/obj + .vscode 예외). (3) **포팅된 코드**: `forge/com_helpers.py` → `Forge.Core/ComHelpers.cs` (5단계 패턴 SetParam/InsertText/Run/단위변환 1:1, `dynamic` 사용). `forge/hwp_session.py` → `Forge.Core/HwpSession.cs` (430줄, `ole32.dll` P/Invoke 로 `IRunningObjectTable` enumerate, `!HwpObject.{ver}.{idx}` 정규식, ShellExecute 우선 spawn + CoCreate fallback, multi-instance picker, `NoExistingHwpException`/`MultipleHwpInstancesException` 보호). `Forge.Probe/Program.cs` (STA 스레드 + `list`/`insert` 서브커맨드). (4) **검증**: 전체 솔루션 빌드 0 경고 0 오류. `Probe list` 정상(한/글 미실행 경로), `Probe insert` 정상(사용자 실측 — 활성 한/글 문서에 텍스트 1줄 박힘). **다음 W2**: parser + renderers 9종 + HwpxWriter 디스패처 포팅.
- 2026-05-18 +09:00 | Claude Code | **TableRenderer (9번째 렌더러) 신설 — GFM 표 → 한/글 표**. 의사결정(§0 spec/table-renderer-plan.md) 후 4 phase 일괄 구현. (1) **parser.py** — `NodeType` 에 `table` 추가 + `Node` 에 `headers`/`rows`/`aligns` 필드 + `TABLE_ROW_RE`/`TABLE_SEP_RE` 정규식 + `_parse_table`/`_split_row`/`_parse_aligns` 3 함수 신설. `_parse_body` 에 표 분기 추가 (callout 분기 뒤, 단일라인 분기 앞). 셀 수 < 헤더 → '' 패딩, > 헤더 → 슬라이스. T1·T3·T4·T5 케이스 검증 통과. (2) **primitives.py** — `escape_table(hwp)` 1 함수 신설 (CloseEx + MoveDown + CloseEx, tool2 `표탈출` line 913-918 1:1). 기존 6 표 헬퍼 (make_table·set_cell_margin_zero·set_table_border_color/thickness/inner_*/bg·move_table_right) 는 이미 tool2 권위와 일치해 신규 추가 0. (3) **templates.py** — `TableStyle` dataclass + `ReportSpec.table` 필드. 1열 25mm, 행 8.4mm, 외곽·내부선 RGB(62,87,165) 굵기 6, 헤더 라벤더 + HY헤드라인M 12pt + 가운데, 데이터 휴먼명조 12pt + 좌정렬(aligns 따름). (4) **renderers/table.py** — TableRenderer 신설. tool2 `행안부초록표`(line 3053+) 권위 패턴 1:1 — 셀별 (표배경색 헤더만) + set_font + align + insert_text + move_table_right, 마지막 셀에서 호출 생략(자동 행 생성 방지), 종료 escape_table + align('justify'). (5) **dispatcher** — `hwpx_writer._dispatch_one` 에 table 분기 1줄 + `renderers/__init__.py` export. cursor 모드(Ctrl+Shift+X selection→md) 자동 지원. (6) **문서** — markdown-spec.md §9 표 신설 + 변경이력 1.6, renderer-spec.md §2.9 TableRenderer 신설, CLAUDE.md §3.4 디렉토리 트리 9 종으로 갱신. spec/table-renderer-plan.md 는 의사결정 완료 마킹. **MCP 권위 검증 4건 완료** (표만들기·표탈출·행안부초록표·TableRightCellAppend usage). **다음**: 사용자 GUI 변환 검증 — 산출물 18개 표 한 페이지씩 시각 확인 (Phase 4 체크리스트는 plan §8).
- 2026-04-30 +09:00 | Claude Code | **폰트·hotkey·prepend 4건 일괄 변경**. (1) **TH휴먼명조 default 화** — 기존 default 폰트 "휴먼명조" 가 실 환경에 미설치라 폴백 표시되는 문제. `forge/formatter/templates.py` 의 `date_font`·`bullets[*].font`·`conclusion_font` 모두 "TH휴먼명조" 로 교체, `realtime_tab.var_font1` 도 동일. `_run_font_apply_async` 에 `set_param` 직후 CharShape readback 진단 로그 추가 (`hwp.HParameterSet.HCharShape.FaceNameHangul` 와 요청 폰트 비교, 불일치 시 ⚠ 경고). settings_tab 라벨도 갱신. (2) **마커 뒤 (개요)/(요약) 자동 강조 폰트 = TT HY울릉도M, 전 레벨 적용** — `parser.py` 의 SUMMARY_RE 매칭을 L1 □ 한정 → L1~L4 모든 글머리(○ - ·) 로 확장. `bullet.py` 는 `SUMMARY_FONT = "TT HY울릉도M"` 클래스 상수 + level 무관 `if summary:` 분기로 정리 (기존 맑은 고딕 Bold 강조에서 폰트만 교체). (3) **단축키 추가 + 재배치** — Ctrl+Shift+D = 새 동작 "선택영역 → TT HY울릉도M 15pt" (요약 부분 빠른 통일용), Ctrl+Shift+F = 기존 D 의 "현재 문단 글자크기 (빈줄용)". hotkey 총 7개 → 8개 (D·F·Z·X). 영향: `app.py` `_setup_hotkeys`, `realtime_tab.py` (var_hk_letter dict 8키 확장, grid 행 4·5 신설/재배치, 행 8·9 = 개별기능 토글), `hotkey_summary_font` 어댑터 신설. CLAUDE.md §2 단축키 표 동기화. (4) **dispatcher 중심의 일관 prepend** — 기존 정책 "md 소스 빈줄 1:1 emit" 으로는 소스에 빈줄 없을 때 변형 사이 spacing 누락. `_dispatch_nodes` 가 모든 비빈 노드 사이에 정확히 1줄 8pt 빈 단락 보장 (소스 빈줄은 coalesce, 첫 노드 앞 자동 prepend 안 함). `initial_prev_emitted` 인자로 메타데이터 헤더 직후 본문 spacing 보장. section/subsection/conclusion/note_callout/attachment 의 내부 "위 빈 줄" emit 코드는 제거 (dispatcher 단일 책임).
- 2026-04-28 +09:00 | Claude Code | **디렉토리 구조 리팩토링 — `forge/` 단일 src 패키지화**. (1) `ui/` 를 `forge/ui/` 로 이동 (src 패키지 일원화). (2) `forge/stage_1_formatter/` → `forge/formatter/`, `forge/stage_2_linter/` → `forge/linter/` (stage_X prefix 제거). (3) `forge/stage_3_polisher/` 빈 폴더 삭제 — 자간조정·들여쓰기 정렬·자간 0 reset 등 STAGE 3 룰들이 실제로는 모두 `linter/` 안에 흡수되어 있어서 별도 패키지 불필요. (4) import 경로 일괄 치환 (.py 7개), `pyproject.toml` entry point + packages 갱신, `run.py` 갱신. (5) CLAUDE.md §6 디렉토리 트리 + §2 구현 언어 표 동기, spec/renderer-spec.md 모듈 경로 갱신. compile + import smoke test 통과.
- 2026-04-27 +09:00 | Claude Code | **한/글 attach 정책 재작성 — ROT enum 패턴 적용 (한컴 공식 권장)**. 사용자 보고: 한/글 띄운 상태에서 Forge "연결" 버튼 누르면 매번 새 한/글 spawn (3개까지 누적). 진단: (1) `GetActiveObject` 는 ROT 직접 조회인데 한/글이 GUI 로 띄워질 때 ROT 등록 안 함 → 항상 실패. (2) fallback 의 plain `Dispatch` 도 HWP 가 REGCLS_MULTIPLEUSE 미등록이라 매번 새 spawn. (3) `EnsureDispatch` 는 항상 신규. → 검증으로 확정: `pythoncom.GetRunningObjectTable().EnumRunning()` 결과 한/글 GUI 떠있어도 0개, 우리 `EnsureDispatch` 직후엔 `!HwpObject.80.1` 등록 확인. AccessibleObjectFromWindow + OBJID_NATIVEOM 도 한/글 미지원 (Office 전용). 한컴 공식 답변(forum.hancom.com) 발견: ROT moniker 패턴 = `!HwpObject.{버전}.{인덱스}` (버전 80=2010, 90=2014, 96=NEO, 100=2018, 110=2020, 120=2022, 130=2024). **수정**: `forge/hwp_session.py` 전면 재작성 — (a) `pythoncom.GetRunningObjectTable()` enum + 정규식 `^!HwpObject\.(\d+)\.(\d+)$` 매칭 (모든 버전 동시 커버, 미래 버전도 fallback `한/글 v{code}`), (b) 매칭 시 `GetObject(mk).QueryInterface(IID_IDispatch)` → `gencache.EnsureDispatch(disp)` 로 attach, (c) 없으면 신규 spawn 후 ROT moniker 차집합으로 자기 인스턴스 식별 (PyIDispatch `_oleobj_` 동등성 비교 신뢰 못함). HwpSession 에 version_code/instance_index/moniker_name 필드 추가 → status 메시지에 `[한/글 2010 #1]` 같은 식별자 노출. **제약 명시**: ROT 는 동일 integrity level 안에서만 enum 가능 → Forge 가 admin (High IL) 으로 실행되면 일반 (Medium IL) 사용자 한/글에 attach 불가. 사용자 검증: Medium IL Forge ↔ Medium IL 한/글 정상 attach 확인 ("그냥 열은 한글에 그냥 열은 forge 가 붙었습니다"). pyproject.toml 에 psutil>=5.9 의존성 추가 (한/글 프로세스 감지용). **다음**: 필요 시 admin Forge 사용자에게 IL 분리 안내 메시지 추가 (현재 보류).
- 2026-04-26 +09:00 | Claude Code | **CLAUDE.md MCP 활용 의무화 강화 (§11-11 격상 + §12 신설)**. 계기: 직전 BorderShape 사고 — MCP 검색을 거치지 않고 액션명을 추측으로 코드에 넣어 런타임 NoneType 에러 발생. MCP 1회 검색(`search_hwp_action("셀 테두리")` → CellBorderFill id=32) 으로 즉시 답 확인 가능했음. (1) §11-11 "MCP 활용 권장" → "★★★ MCP 우선 의무 — 추측 금지, MCP 먼저" 로 격상. 금지 행위 3종 명시: ① MCP 검색 없이 액션명 직접 작성, ② 디컴파일 텍스트/PDF grep 우회, ③ 결과 없이 인접 액션명 추측. (2) **§12 MCP 활용 워크플로 신설**: §12.1 두 MCP 역할 구분(hwp-api=사전, tool2-spec=용례집), §12.2 5단계 표준 워크플로(tool2 용례 → 한컴 권위 확인 → 파라미터 검증 → primitives 추가 → 렌더러 호출), §12.3 자주 쓰는 호출 패턴, §12.4 실패 사례(BorderShape) 박제, §12.5 MCP 가용성 점검 절차. (3) 발견: tool2-spec MCP 가 .mcp.json 등록은 되어 있으나 이번 세션에서 deferred tool 로 로드되지 않음 — 빌드/연결 점검 필요 (사용자에 별도 보고).
- 2026-04-26 +09:00 | Claude Code | **primitives.py 표 테두리·배경 액션명 1차 수정**. 사용자 변환 실행 중 `'NoneType' object has no attribute 'CreateSet'` 발생 — `set_table_border_thickness` 가 존재하지 않는 액션 `BorderShape` 호출했기 때문. tool2 한컴라이브러리_decompiled.py 라인 690-910 (표내부선*·표배경색·표테두리굵기/타입/색·표테두리단일선) 검증 후 다음 6 함수 재작성: `set_table_border_type`, `set_table_border_thickness`, `set_table_border_color`, `set_table_border_single_line`은 단일 액션 `CellBorderFill` + 정확한 항목명(`BorderTypeTop`/`BorderWidthTop`/`BorderColorTop`/`BorderCorlorLeft`(sic)) 사용. `set_table_bg`는 별도 액션 `CellFill` + `HParameterSet.HCellBorderFill.FillAttr` 직접 설정 패턴 적용. 추가로 `set_table_inner_line_type/thickness/color` 3 함수 신설 (`TypeHorz/Vert`, `WidthHorz/Vert`, `ColorHorz/Vert`). renderers (metadata, section, subsection, conclusion, note_callout, attachment) 호출부는 변경 불필요 — 함수 시그니처 동일. **다음**: 사용자 재실행 + 다른 잠재 액션명 오류 발견 시 동일 방식 수정.
- 2026-04-26 +09:00 | Claude Code | **렌더러 8종 + dispatcher 실제 구현 완료**. spec/renderer-spec.md v0.1 의 Phase 1~4 한 번에 실행. (1) `forge/renderers/base.py` ElementRenderer 추상 + `primitives.py` 36개 COM 헬퍼 (mm/pt/RGB 변환, set_font, set_indent, make_table, set_table_*, align 등). (2) **8 렌더러 모두 구현**: MetadataRenderer (대제목 노란박스 + stamp), SectionRenderer (Ⅰ./Ⅱ. 파란밑줄), SubsectionRenderer (가/나 라벤더박스), BulletRenderer (□○-· 4단계, □ 의 (요약) Bold 강조 포함), AnnotationRenderer (* ※ † 단일 spec), ConclusionRenderer (=> 민트 점선박스), NoteCalloutRenderer ([참고] 파란헤더+흰글씨), AttachmentRenderer ([붙임 N] 페이지break + 헤더). (3) `hwpx_writer.py` 를 dispatcher 로 전면 재작성 — 노드 타입에 따라 적절한 렌더러 호출. (4) **spec v1.4 반영**: markdown_tab 에 작성부서·작성일 입력 필드 추가 (메타바). 변환 시 UI 입력값을 hwpx_writer 에 전달. front-matter 의 보고서명만 markdown 영역. (5) base.py 의 ReportSpec import 를 TYPE_CHECKING 으로 처리해 순환 import 회피. (6) 검증 완료: 8 렌더러 + 36 primitives + dispatcher + UI 모두 정상 import, 샘플 markdown (18 노드) 파싱 정상. **다음**: 실제 한/글에서 변환 동작 확인 (사용자 검증).
- 2026-04-26 +09:00 | Claude Code | **운용 전제 정정 — COM 일원화**. 사용자 명시: "1~3 stage는 1개의 환경에서 작동하는 하나의 툴이고, 모든 환경은 한/글이 깔려있는 것을 상정합니다. hwp com api를 배제할 필요가 없습니다." 원래 설계의 "STAGE 1·2 = 한/글 ✗, lxml로 hwpx 직접 생성" 전제 폐기. **3 stage 모두 한/글 COM API 사용**. 3-stage 분리는 _개념적 관심사 분리_만 유지: STAGE 1 (md 파싱+렌더러 dispatch), STAGE 2 (문서 전체 일관성 룰), STAGE 3 (단일 룰 + 렌더러 재활용 = 실시간 박스 삽입). 영향: CLAUDE.md §2 파이프라인 다이어그램·단계별 요약표·형식 정책·§6 디렉토리 주석·구현 언어 표(lxml/zipfile 제거 + COM 일원화 + 미선택 대안 명시)·§8 F3·F4 로드맵·푸터 모두 정정.
- 2026-04-26 +09:00 | Claude Code | **렌더러 사양 spec/renderer-spec.md v0.1 확정**. 마크다운 요소 8종을 **독립 클래스 8개**로 분리 — MetadataRenderer (대제목+stamp), SectionRenderer (Ⅰ./Ⅱ.), SubsectionRenderer (가./나.), BulletRenderer (□○-·), AnnotationRenderer (*※† 단일 spec), ConclusionRenderer (=> 민트박스), NoteCalloutRenderer ([참고]), AttachmentRenderer ([붙임] 페이지break). **★ STAGE 1 dispatcher와 STAGE 3 실시간 버튼이 동일 렌더러 인스턴스 호출** — "한 벌로 두 모드 완전 커버" 아키텍처. tool2 보고서1 spec authority 100% 매핑 (decompiled.py 라인 번호 모두 명시). Forge 자체 정의 부분(- · 본문 3·4단계, [붙임] 페이지 break) 별도 표시. 공통 헬퍼 forge/renderers/primitives.py 30개 함수 명세. 구현 로드맵 4-Phase 제시. 영향: spec/renderer-spec.md 신규 작성, CLAUDE.md §6 디렉토리에 forge/renderers/ 반영, §10 문서 인덱스에 추가, 푸터 갱신.
- 2026-04-26 +09:00 | Claude Code | **F2 STAGE 1 GUI 본체 1차 구현 완료**. (1) `pyproject.toml` 생성 (Python 3.12, ttkbootstrap + lxml + pywin32 + pyyaml). (2) `forge/` 코어: `com_helpers.py` (set_param 5단계 헬퍼 1개) + `hwp_session.py` (한/글 attach·신규 생성) + `stage_1_formatter/` (templates·parser·hwpx_writer 3 모듈). (3) STAGE 1 hwpx writer는 1차 구현으로 COM API 경유 (한/글 FileNew → 본문 삽입 → SaveAs hwpx). 후속으로 pure XML(lxml+zipfile) 교체 예정. (4) `ui/` GUI: ttkbootstrap "cosmo" 테마, `app.py` 메인 윈도우 + status bar + 3-탭 노트북 + 한/글 백그라운드 attach. (5) **탭 ① 기본정보**: 메타데이터 입력 + 템플릿 선택 + 페이지여백 6방향 + 줄간격 + 폰트 4행 + 글머리 4단계 (□ ○ - ·) — 모두 보고서1 spec 기본값 + 사용자 테일러링. (6) **탭 ② 마크다운 입력**: 좌측 md 에디터 + 우측 진행 로그 + 툴바 (열기·저장·샘플채우기·비우기·변환). 변환은 백그라운드 스레드로 hwpx 생성 (UI 블로킹 방지). (7) **탭 ③ 개별작업**: placeholder + 룰 카테고리별 버튼 골격 (실제 동작은 후속). (8) `run.py` entry. (9) import + GUI 인스턴스화 sanity test 통과. **다음**: 실제 한/글에서 변환 동작 검증, STAGE 1 박스/색상 디테일 추가, 탭 ③ 룰 연결.
- 2026-04-26 +09:00 | Claude Code | **본격 개발 개시 + UI 아키텍처 결정 + git init**. (1) UI = **Full GUI 단일 진입점** 확정 (이전 Tkinter+CLI 듀얼 안 → GUI 단독으로 변경). Tkinter + ttkbootstrap, 3-tab(기본정보/마크다운입력/개별작업), 활성 한/글 프로세스 자동 감지·attach. (2) 디렉토리 구조 갱신: `forge/`(코어 라이브러리, UI 무관) + `ui/`(Tkinter GUI). cli/는 제외. (3) 구현 언어 = Python 3.12 단일 스택 확정 (lxml + zipfile + pywin32 + tkinter + ttkbootstrap). (4) **.gitignore 전면 갱신**: 외부 자산(tool2 .exe·.pdf, FSS .hwp 샘플)·임시 산출물(_rendered, PyInstaller raw extract, 디스어셈블 .txt 7.5MB)·third-party 클론(_tools/pycdc) 제외. 우리 분석본(_decompiled.py·분석노트·spec)만 commit. (5) git init 수행. (6) CLAUDE.md §3 "구현 형태" 신설 + §6 디렉토리/구현 언어 전면 갱신 + 푸터.
- 2026-04-26 +09:00 | Claude Code | **markdown spec v1.3 확정** — tool2 금감원페이지 spec과 1:1 매핑 가능해짐. (1) **소제목 신설**: 레벨 2로 `가.`/`나.`/.../`하.` 추가, 기존 `□`/`○`/`-`/`·`는 레벨 3~6으로 한 단계씩 밀림. 섹션과 □ 사이 중간 분류, 사용 자유. 소제목명 자체가 요약 역할이라 별도 요약단어 불필요. (2) **Callout 박스 신설 (§8)**: `[참고]` (본문 중간 보충 박스, tool2 참고 박스 매핑) + `[붙임]`/`[붙임 N]` (별도 페이지 첨부, tool2 별지 매핑). 라인 시작 단독 마커, 다음 빈 줄까지 본문. (3) 영향: spec/markdown-spec.md 양쪽(Forge + Sentinel) 동기 갱신, CLAUDE.md §4 요약+§5 시각 룰 표 갱신, 푸터 기록. **후속 필요**: Sentinel STAGE 2 시스템 프롬프트에 소제목+callout 사용 안내 추가.
- 2026-04-26 +09:00 | Claude Code | **사용자 관점/내부 구조 분리 명시**. 사용자 노출 인터페이스는 정확히 2 모드 (배치 = md→hwpx 완성, 실시간 = 활성 문서 polishing). STAGE 1·2 단독 호출 모드(lint-only) 명시적 미제공 결정 — 사용자 단순성 우선. 내부 3-stage 분리는 관심사 분리·테스트·재사용 목적의 구현 상세. 영향: README.md 사용 모드를 Stage 구분보다 앞으로 배치 + Stage 구분 → "내부 구현 (참고)" 후방 배치, CLAUDE.md §2 도입부에 "사용자 관점 vs 내부 구조" 문단 + §3에 모드↔stage 매핑표 추가.
- 2026-04-26 +09:00 | Claude Code | **형식 정책 (HWP/HWPX) 정식 채택**. 정부의 HWP 단계적 퇴출 의사결정 반영. 정책: ① 신규 산출 = .hwpx 강제 (배치 모드 출력), ② 레거시 .hwp 입력 호환 유지 (실시간 모드), ③ STAGE 3 COM 호출은 형식 무관 (HwpFrame.HwpObject 가 자동 처리, 룰 코드 분기 없음), ④ STAGE 1·2 = HWPX 전용 (바이너리 .hwp 파서/생성기 영원히 불필요). 영향: CLAUDE.md §1·§2 (파이프라인·요약표·신설 "형식 정책" 서브섹션)·§3 (조합 사용 예)·§6 (디렉토리 주석)·푸터 갱신, README.md 기능·Stage·사용 모드·신설 "형식 정책" 섹션 갱신. 또한 라이브러리 스택 확정(lxml + zipfile + win32com.client + 1줄 헬퍼, pyhwpx 미도입), Python 3.12, UI 아키텍처 = Tkinter + CLI 듀얼 (forge core 라이브러리 공유) 결정.
- 2026-04-26 +09:00 | Claude Code | **F1.5 완료 + CLAUDE.md 대폭 갱신**. (1) tool2(금감원 오피스 프로그램) 완전 분해: PyInstaller 추출 → pycdc 4개 패치 (`data.cpp`의 `std::exit(1)` 제거가 결정적) → 411 메서드 100% Python 디컴파일 + `메인.pyc` 939줄. (2) MariaDB `tool2_spec_db` 7 테이블 스키마·seed 적재 (411 메서드 + 5 템플릿 + 361 진입절차 + 1623 액션사용 + 20 마크다운 directive). (3) `dev-support/tool2-spec-mcp/` 생성: TypeScript MCP 서버 7 도구(search_tool2_methods/get_tool2_method/get_tool2_method_source/list_tool2_templates/get_tool2_template/list_tool2_directives/get_tool2_action_usage), .mcp.json 등록, smoke test 통과. (4) **마크다운 spec 의사결정**: tool2 directive (`네모:내용` 등) 미지원 확정. Forge 글머리표 spec(`□ ○ - ·`) 단독 유지. 결정 근거: 인지 부담·LLM 친화성·호환성·계약 안정성·차용 가치 부재. (5) **tool2 spec authority 원칙 명문화**: 입력 spec=Forge 자체 / 시각 출력 spec=tool2. (6) CLAUDE.md §1·§3·§4·§5·§6·§7·§8·§9·§10·§11 모두 갱신, F1.5 신설, AI 협업 지침 9·10·11 추가. (7) 분석노트 §12 (최종 결론) 작성. **다음**: F2 STAGE 1 HWPX Formatter 착수.
- 2026-04-26 +09:00 | Claude Code | **명명 규약 갱신**: STAGE A/B/C → STAGE 1/2/3. 사용 모드도 3개(배치/단독/실시간 동반자) → 2개(배치/실시간)로 단순화 — 배치/단독은 사실상 동일한 풀 파이프라인이고 입력 출처(Sentinel/손글)만 다른 것이므로 구분 폐기. 영향 파일: `CLAUDE.md`(파이프라인 다이어그램·요약표·§3 사용 모드·§5 시각룰 표·§6 디렉토리 구조·§8 로드맵·§9 Phase 상태판·§11 협업 지침), `README.md`(Stage·모드 섹션), `spec/markdown-spec.md`(STAGE A 파서 → STAGE 1 파서) + Sentinel 측 사본 동기 갱신, `.gitignore`(주석). README/.gitignore 신설(이번 세션 시작 시).
- 2026-04-25 +09:00 | Claude Code | **F0 자산 이관 완료**. (1) `sentinel/hwp_api_reference/official_api/*` → `sentinel-forge/reference/official_pdfs/` (PDF 4종 + txt 4종). tool1/tool2 빈 placeholder 폴더도 `reference/`로 보존. (2) `sentinel/mcp/hwp-api-mcp/` → `sentinel-forge/dev-support/hwp-api-mcp/`. (3) `sentinel/scripts/{extract-hwp-api.py, parse_hwp_api.py, hwp_api_schema.sql, smoke_test_hwp_mcp.py}` → `sentinel-forge/scripts/`. 이동된 스크립트의 상대경로 3곳 갱신(`reference/official_pdfs`, `dev-support/hwp-api-mcp`). (4) Sentinel `.mcp.json`에서 `hwp-api` 항목 제거, Forge `.mcp.json` 신규 생성. (5) hwp-api-mcp 새 위치에서 npm run build 통과 + smoke test 6 도구 정상 응답. F0 체크리스트 5/7 완료(SQLite export·hdbuser 정리는 F5로). F1 체크리스트도 완료 표시.
- 2026-04-25 +09:00 | Claude Code | md 사양 v1.1 확정. Bold(`__X__`) 추가, 이탤릭 금지, asterisk는 참조 전용으로 분리(다중 참조 `**`/`***`와 markdown bold 충돌 해결). 들여쓰기 규칙 삭제(라인 시작 글머리 문자만으로 식별). `*`/`※` 공존, 결론 화살표 위치 자유, 섹션 헤더 앞 빈 줄 의무화 명문화. § 5 시각 룰 표에 Bold 변환 행 갱신.
- 2026-04-25 +09:00 | Claude Code | md 사양 v1.0 확정. 결정 미정 항목 모두 제거하고 5종 의미(메타데이터·층위·요약단어·주석·결론 화살표)만 명문화. 350줄+ → 174줄로 단순화. Sentinel STAGE 2 프롬프트 갱신 필요 항목(front-matter 출력, □ 요약단어 의무화 등) 변경 이력에 기록. 양 프로젝트 사본 동기화 확인 완료.
- 2026-04-25 +09:00 | Claude Code | md 사양 스켈레톤을 양 프로젝트에 미러링. `spec/markdown-spec.md` ↔ `sentinel/docs/markdown-spec.md` 동일 내용 유지 규약 추가(상단 동기화 배너 + 양쪽 CLAUDE.md AI 협업 지침에 동기화 의무 명시). 두 파일은 바이트 단위 동일.
- 2026-04-25 +09:00 | Claude Code | `spec/markdown-spec.md` 스켈레톤 작성. Sentinel STAGE 2 현 출력 기준으로 ✅ 결정 항목(섹션 골격, 5단계 글머리표, 각주 표기, 법령 인용 등)과 ❓ 결정 필요 항목(front-matter, (요약단어) 표기, 각주 다중 처리, Bold 정책, 표 사양, 위반 시 동작 등 10여 개) 분리 정리. 우선순위 가이드 + Sentinel 정합성 체크리스트 포함.
- 2026-04-25 +09:00 | Claude Code | 프로젝트 신설. Sentinel CLAUDE.md에서 STAGE 3·4·5(HWPX 작성·정규화·COM 폴리싱)와 관련 자산(hwp_api_reference, hwp_api_db, hwp-api-mcp, 추출/적재 스크립트)을 본 프로젝트로 분리. 결합점은 개조식 md 사양 하나로 한정. CLAUDE.md·TASK_LOG.md 신설. F0 자산 이관 대기 상태.
